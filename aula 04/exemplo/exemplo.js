let numeros = [10, 20, 80, 90];

/* const callback = (acc, next, index, array) => {
  console.log('----------');
  console.log(acc);
  console.log(next);
  console.log(index);
  console.log(array);

  return acc;
};
numeros.reduce(callback, 0); */
